name: CI
on:
  push:
    branches:
      - main

jobs:
  ci-publish:
    uses: quentinadam/deno-ci-workflow/.github/workflows/ci-publish.yml@v1
    permissions:
      contents: write
      id-token: write
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
